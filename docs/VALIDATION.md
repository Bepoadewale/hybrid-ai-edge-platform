# Validation

Run `PYTHONPATH=control-plane/src python3 -m pytest -q`. Live platform status requires recorded independent agent processes, actual ONNX Runtime execution/provider, cryptographic signature/digest verification, control-plane interaction and explicit offline/rollback evidence. Simulated hardware values must remain labelled.
